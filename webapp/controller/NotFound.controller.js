sap.ui.define([
    "etiqueta/ubicacion/controller/App.controller"
],
function (Controller) {
    "use strict";

    return Controller.extend("etiqueta.ubicacion.controller.NotFound", {
        onInit: function() {
        }
    });
});
